import authAppHeadingVue from "./auth-app-heading.vue";
import authHeadingVue from "./auth-heading.vue";
import authPanelVue from "./auth-panel.vue";
import authRootVue from "./auth-root.vue";

export const auth = {
  root: authRootVue,
  appHeading: authAppHeadingVue,
  heading: authHeadingVue,
  panel: authPanelVue,
};
